<Window x:Class="AvdLauncher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:AvdLauncher.ViewModels"
        mc:Ignorable="d"
        Title="Azure Virtual Desktop Launcher" Height="720" Width="1120">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Padding="12" BorderBrush="#DDD" BorderThickness="1" CornerRadius="8" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Tenant / App Registration" FontWeight="SemiBold" FontSize="14" Margin="0,0,0,8"/>
                <UniformGrid Rows="2" Columns="2" Margin="0,0,0,8">
                    <StackPanel Margin="0,0,8,4">
                        <TextBlock Text="Tenant ID"/>
                        <TextBox Text="{Binding TenantId, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Margin="8,0,0,4">
                        <TextBlock Text="Client ID"/>
                        <TextBox Text="{Binding ClientId, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Margin="0,4,8,0">
                        <TextBlock Text="Subscription ID"/>
                        <TextBox Text="{Binding SubscriptionId, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Margin="8,4,0,0">
                        <TextBlock Text="Resource Group"/>
                        <TextBox Text="{Binding ResourceGroup, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </UniformGrid>
                <TextBlock Text="Tip: keep this app and Windows App signed in with the same Entra account. MSAL broker + WAM enables silent SSO for launch." Foreground="#555" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Add user (Microsoft login)" Width="210" Margin="0,0,8,0" Command="{Binding AddUserCommand}"/>
            <Button Content="Refresh assigned apps" Width="180" Margin="0,0,8,0" Command="{Binding RefreshAppsCommand}"/>
            <ComboBox Width="380"
                      ItemsSource="{Binding Users}"
                      DisplayMemberPath="DisplayName"
                      SelectedItem="{Binding SelectedUser}"
                      IsEditable="False"/>
        </StackPanel>

        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding RemoteApps}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column"
                  RowHeight="52"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Launch" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="▶"
                                    ToolTip="Launch in Windows App"
                                    FontSize="18"
                                    Width="42"
                                    Height="32"
                                    Command="{Binding DataContext.LaunchAppCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="App" Binding="{Binding Name}" Width="2*"/>
                <DataGridTextColumn Header="Workspace" Binding="{Binding WorkspaceName}" Width="2*"/>
                <DataGridTextColumn Header="App Group" Binding="{Binding ApplicationGroupName}" Width="2*"/>
                <DataGridTextColumn Header="Host Pool" Binding="{Binding HostPoolName}" Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="#0A4" Margin="0,12,0,0" TextWrapping="Wrap"/>
    </Grid>
</Window>
